gridSide = 20px
nav.nav
  display inline
  ul
    display inline-block
    vertical-align: top
    margin: 0
    position: relative
    z-index: 1
    li
      display inline
      list-style: none

nav.nav
  ul#ctrl-links
    width gridSide
    height gridSide
    margin gridSide
    > li
        // width gridSide
        float left;
        clear both
        height gridSide
        display block
        margin-bottom gridSide
      form
        display inline
        position relative
        float left
        > input, label
          top 0
          left 0
          position: absolute
          display block
          width gridSide
          height gridSide
          box-sizing: border-box
        label
          border 1px solid COLOR_DARK_BLACK;
        input[type=checkbox]
          z-index 1
          opacity 0
          cursor pointer
          &:hover+label
            background-color COLOR_GREY
          &:checked+label
            background-color: COLOR_DARK_BLACK
        > span
          font-size 1em * 0.5
          display block
          margin-left gridSide * 2
        > span:not(:first-child)
          width: 200px

#main-menu
  #credits-link
    position: fixed
    top gridSide - 10px
    right gridSide


nav.nav
  ul#main-menu
    margin-top 12px

#credits
  #home-link
    position relative
    top 20px
    left 30px